#include <iostream>
#include <vector>
#include <string>
#include <list>
#include <algorithm>
#define MOD 1000000007
#define INF 1 << 30

using namespace std;
typedef long long ll;

int main(void) {
	int num, m, i, a, b;
	cin >> num >> m;
	vector<list<int>> mp(num + 1);
	for (i = 0; i < num; i++) {
		cin >> a >> b;

	}
	return 0;
}